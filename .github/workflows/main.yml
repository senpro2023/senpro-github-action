name : Test, Build, and Deploy, | Modul 01 - Lab2.2 Senior Project

on :
  push:
    branches: [main]
jobs:
  test-build:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [16.x]

    steps:
      - uses : actions/checkout@v2
      - name : Testing Build pre-Deploy
        uses : actions/setup-node@v2
        with :
          node-version: ${{ matrix.node-version }}
<<<<<<< HEAD
          cache: "npm" 
      - run : npm i
      - run : npm run build 

    deploy:
      needs: test-build
      runs-on : ubuntu-latest

    strategy :
      matrix :
        node-version: [16.x]

    steps :
    - name : Build app on VM
      uses : appleboy/ssh-action@master
      with:
        host: ${{ secrets.HOST }}
        username: ${{ secrets.USERNAME }}
        password :  ${{ secrets.Password }}
        port :  ${{ secrets.PORT }}
        script: |
          eval "$(ssh-agent -s)"
          ssh-add ~/.ssh/senpro-ssh
          cd ~/senpro/460553/modul01/senpro-github-action
          git restore
          git pull origin main
          npm install
          npm run build

=======
          cache: "npm"
      - run : npm i
      - run : npm run build 
>>>>>>> 0651f3bb51272166fef677b8d74b21fe2478549f
